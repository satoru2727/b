---
const { path, blogtitle, date, emojidata} = Astro.props;
const tt = date;
---
<li class="flex flex-row m-2.5 h-fit">
<img src={emojidata} alt="" class="mt-1.5 p-2.5 h-full bg-white rounded-xl max-w-[30%]" href={path}>
<div class="ml-2.5 my-auto mx-auto">
<a href={path} class="text-3xl font-semibold">{blogtitle}</a><br>
<a　class="text-slate-500" href={path}>{tt}</a>
</div>
</li>
